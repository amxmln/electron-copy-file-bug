{
  "name": "copy-test",
  "version": "1.0.0",
  "description": "Testing fs.copyFile in an Electron application when built with ASAR",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "electron-builder --dir"
  },
  "build": {
    "asar": true
  },
  "author": "Amadeus Maximilian",
  "license": "ISC",
  "devDependencies": {
    "electron": "^2.0.8",
    "electron-builder": "^20.28.2"
  }
}
